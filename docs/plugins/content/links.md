# 友情链接页面

友情链接页面统一承载友链、RSS 动态、申请和留言状态，并通过 URL 查询参数恢复当前视图。

| 项目     | 说明                                                         |
| -------- | ------------------------------------------------------------ |
| 路由     | `/links`                                                     |
| 设置位置 | `外观 -> 主题 -> Sky Blog -> 设置 -> 友链`                   |
| 依赖插件 | `PluginLinks 2.2.1`；留言可选 `PluginCommentWidget >= 3.1.2` |

## 页面内容

| 区域     | 做什么                                           |
| -------- | ------------------------------------------------ |
| 页面头部 | 显示标题和描述                                   |
| 友链列表 | 按插件返回的分组展示站点卡片及链接检测状态       |
| 友链动态 | 展示 RSS 来源、公开动态和有权限用户的个人状态    |
| 本站信息 | 弹窗展示本站名称、描述、Logo、RSS 等             |
| 申请入口 | 识别站点；管理员可直连创建，其他用户生成留言申请 |
| 评论区   | 页面底部评论，可关闭                             |
| 侧边栏   | 可使用全局侧边栏，也可单独配置                   |

## 页面路由

| 状态                        | 路由                                                  | 权限                                     |
| --------------------------- | ----------------------------------------------------- | ---------------------------------------- |
| 全部友链 / 分组 / 详情      | `/links`、`?group=`、`?link=`                         | 公开                                     |
| RSS 来源                    | `?view=friends`                                       | 公开                                     |
| 全部 / 未读 / 收藏 / 稍后读 | `?view=friends&scope=...`                             | 全部公开；个人状态需 `plugin:links:view` |
| RSS 分组 / 单一来源         | `?view=friends&groupName=`、`?view=friends&linkName=` | 公开                                     |
| RSS 内部详情                | 有效动态路由追加 `&itemId=`                           | `plugin:links:view`                      |
| 添加或修改申请              | `?view=apply`                                         | 公开；直连创建需 `plugin:links:manage`   |
| 留言板                      | `?view=board`                                         | 页面公开；提交依赖评论组件               |

运行时只管理 `view`、`group`、`link`、`scope`、`groupName`、`linkName`、`itemId` 七个参数。未知查询参数和 hash 保留，非法组合通过 `history.replaceState` 规范化；canonical 始终为 `/links`。

## 全量配置项

### 基础信息 `basic_info`

| 设置项   | 字段               | 说明                       |
| -------- | ------------------ | -------------------------- |
| 页面标题 | `page_title`       | 友链页面顶部主标题         |
| 页面描述 | `page_description` | 友链页面顶部说明文字       |
| 启用评论 | `enable_comment`   | 是否在友链页底部显示评论区 |

### Links 2.2 展示 `display_settings`

| 设置项   | 字段               | 说明                                                   |
| -------- | ------------------ | ------------------------------------------------------ |
| 链接状态 | `show_link_status` | 显示 PluginLinks 访问检测状态；没有检测结果时不显示    |
| 友链动态 | `show_link_feeds`  | 显示公开 RSS 动态；还需在 PluginLinks 后台开启公开动态 |

RSS 首屏和后续分页固定每次读取 20 条，使用 `beforePublishedAt + beforeId` 复合游标，不写入页面 URL。

### 申请与本站信息 `apply_settings`

| 设置项       | 字段                  | 说明                      |
| ------------ | --------------------- | ------------------------- |
| 启用申请入口 | `enable`              | 是否显示友链申请入口      |
| 申请区标题   | `apply_title`         | 友链申请区域标题          |
| 申请区描述   | `apply_description`   | 说明申请条件或交换规则    |
| 申请地址     | `apply_url`           | 申请按钮跳转地址          |
| 本站 Logo    | `blogger_logo`        | 提供给对方添加友链使用    |
| 本站名称     | `blogger_title`       | 提供给对方添加友链使用    |
| 本站 RSS     | `blogger_rss`         | RSS 地址，常用 `/rss.xml` |
| 联系邮箱     | `blogger_email`       | 友链申请或联系邮箱        |
| 本站描述     | `blogger_description` | 提供给对方添加友链的描述  |

### 侧边栏 `sidebar_settings`

| 设置项     | 字段                    | 说明                     |
| ---------- | ----------------------- | ------------------------ |
| 显示侧边栏 | `show_sidebar`          | 是否显示友链页侧边栏     |
| 定制侧边栏 | `enable_custom_sidebar` | 开启后覆盖全局侧边栏配置 |
| 侧边栏位置 | `sidebar_position`      | 友链页侧边栏在左侧或右侧 |
| 小工具列表 | `sidebar_widgets`       | 友链页侧边栏小工具列表   |

### Dock `floating_dock`

| 设置项       | 字段                 | 说明                       |
| ------------ | -------------------- | -------------------------- |
| 定制悬浮栏   | `enable_custom_dock` | 开启后单独配置友链页 Dock  |
| Dock 菜单    | `dock_menu`          | 选择友链页 Dock 使用的菜单 |
| 返回顶部按钮 | `enable_back_to_top` | 是否显示返回顶部入口       |

## 基础设置

| 设置项     | 说明                   |
| ---------- | ---------------------- |
| 页面标题   | 页面顶部主标题         |
| 页面描述   | 页面顶部说明文字       |
| 申请区标题 | 友链申请区域标题       |
| 申请区描述 | 说明申请条件或交换规则 |

## 本站信息

| 设置项   | 说明                    |
| -------- | ----------------------- |
| Logo     | 留空时使用站点默认 Logo |
| 站点名称 | 用于对方添加友链        |
| 站点描述 | 用于对方添加友链        |
| RSS 地址 | 默认可填 `/rss.xml`     |
| 联系邮箱 | 留空时不显示            |

这部分只用于展示和复制，不会自动提交到对方站点。

## 识别、创建与安全降级

- 页面先读取当前 Halo 用户和 UI 权限，不用前端判断替代服务端鉴权。
- 有 `plugin:links:manage` 时，自动识别调用 PluginLinks 的 `links/-/detail` 与 `rss/discovery`，新增申请通过 Halo 标准 `core.halo.run/v1alpha1/links` 创建，并携带 CSRF 令牌。
- 官方识别失败后才尝试浏览器匿名识别；请求限制为 HTTP/HTTPS、8 秒、1.5 MB 和 HTML/XHTML，仍可能受目标站 CORS 限制。
- 访客、无管理权限、401/403 安全降级，以及所有修改申请，都只生成 Markdown，不调用受保护写入接口；复制后切换到留言板。
- 主题不调用 `plugin-friends`、`link-submit` 或 `/friends`。

配置的 `apply_url`、评论和邮箱仍作为页面上的辅助联系入口；站内表单的能力判断和降级规则不依赖这些入口。

## PluginLinks 2.2.1 契约

- 页面标题保留主题自定义值；留空时使用插件提供的 `linksTitle` 安全回退。
- `/links?group={metadata.name}` 使用 `simpleGroups`、`groups`、`group` 和 `links` 路由变量，不读取已废弃的 `LinkGroup.spec.links`。
- 真实空分组显示“该分组暂无友情链接”，不存在的 `group` 显示“未找到该友链分组”。
- `LinkVo.status` 的访问和反链状态在 Thymeleaf 中是 Java 枚举，模板先调用 `name()` 再映射显示；状态为空时不误报为离线。
- `linkFeedFinder.groupBy(1)` 提供 RSS 来源首页；只有 `scope=all`、`groupName` 或 `linkName` 路由会由 SSR 预取 20 条公开动态。
- 公共列表使用匿名 `/apis/api.link.halo.run/v1alpha1/linkfeeds`；有查看权限时自动切换到受保护 RSS 列表，并开放未读、收藏、稍后阅读、内部详情与状态写入。
- 打开内部详情会把条目标记为已读，并移出稍后阅读；收藏状态不会自动改变。
- 页面入口通过共享 PJAX 生命周期挂载和清理，前进、后退由 `popstate` 恢复。

## 侧边栏与 Dock

| 设置项       | 说明                     |
| ------------ | ------------------------ |
| 显示侧边栏   | 页面改为左右布局         |
| 定制侧边栏   | 开启后覆盖全局侧边栏     |
| 侧边栏位置   | 左侧或右侧               |
| 小工具列表   | 选择作者卡片、统计等组件 |
| 定制悬浮栏   | 开启后覆盖全局 Dock      |
| 返回顶部按钮 | 控制是否显示返回顶部入口 |

## 插件注意事项

| 场景         | 建议                                                            |
| ------------ | --------------------------------------------------------------- |
| 页面打不开   | 确认 `PluginLinks >= 2.2.1` 已安装并启用                        |
| 友链为空     | 到插件后台添加友链和分组                                        |
| 状态不显示   | 先在 PluginLinks 后台执行链接检测；未检测状态会被主题主动隐藏   |
| 动态不显示   | 开启插件的“公开 RSS 订阅动态”，并确认友链已配置有效 RSS         |
| 识别失败     | 浏览器识别受 CORS 限制时手动填写；管理员同时检查 Links 管理权限 |
| 不能直接创建 | 确认账号具有 `plugin:links:manage`；否则使用生成的留言申请      |
| 留言不可用   | 确认 `PluginCommentWidget >= 3.1.2` 已启用                      |

## 灯箱

友情链接页面不是图片浏览页，通常不需要配置 lightgallery.js 灯箱。
