# v2.2.39：分类分页、友情链接与代码块修复

## 修复

- [#40](https://github.com/sky121666/halo-theme-sky-blog-1/issues/40)：分类页停止把下一页文章无限追加到当前页。分类切换和翻页使用真实链接，文章数、地址和分页器保持一致。
- [#38](https://github.com/sky121666/halo-theme-sky-blog-1/issues/38)：移除旧版 Links 分组字段调用，修正首页友链分组筛选的 Thymeleaf 作用域，并补齐 Halo 标准布局 `templates/layout.html`，避免友链页因 `EL1008E` 返回 500。
- [#36](https://github.com/sky121666/halo-theme-sky-blog-1/issues/36)：让文章中的 `pre code` 保留代码块原有空白与换行，修复多行代码挤成一行。

## 插件与页面适配

- 支持 PluginLinks 2.3.0 的访客申请接口与权限门控；原独立 Friends 页面退役，友链动态统一位于 `/links?view=friends`。旧菜单中的 `/friends` 项会指向新入口。
- 更新 Douban 数据字段兼容、页面切换时的组件清理、文章热力图及分类/标签异常页码处理。插件相关验证结果和未覆盖场景见[适配记录](../system/adaptation/current-plugin-results.md)。
- 主题声明的最低 Halo 版本仍为 2.23.0；本次最新插件组合在本地 Halo Pro 2.26.1 上验证。各插件自己的最低 Halo 要求仍须分别满足。

## 验证

- Node.js 24.19.0、pnpm 10.34.5：冻结依赖安装、lint、格式检查、依赖审计、完整构建及主题 ZIP 二次检查通过；静态插件契约 27 项通过。
- Halo Pro 2.26.1、PluginLinks 2.3.0、本版主题代码：插件页面和公开 API 深度读取检查 **33/33** 通过。
- 真实浏览器避开旧页面缓存后，`/categories` 首屏和滚动后均为 6 篇，第二页是另外 6 篇；`/links` 正常显示；含 26 个代码块的文章中，抽样多行代码的计算样式为 `white-space: pre`。[精确环境与结果](../system/adaptation/evidence/2026-09-24/release-issues-validation.json)

本次验证覆盖上述主题页面和读取链路。插件后台写入、第三方 RSS 可达性，以及此前偶发的 Bilibili 上游超时和装备插件分页告警，不包含在这三项主题修复的通过结论中。
